# Project Structure

## Monorepo Layout

Redandan uses a **bun workspaces** monorepo. Each platform lives in `apps/` as its own workspace.
The root holds workspace configuration, shared tooling, and documentation.

```
redandan/                         ← Monorepo root
  apps/
    web/                          ← Next.js app (@redandan/web)
    extension/                    ← Browser extension (@redandan/extension) — Phase 6
  docs/                           ← Documentation (this folder)
  package.json                    ← Workspace root — scripts delegate to apps/
  bun.lock                        ← Shared lockfile across all workspaces
  .gitignore
  .prettierrc
  .prettierignore
  AGENTS.md
  PLANNING.md
  .env.example
```

Workspaces are declared in the root `package.json`:
```json
{
  "workspaces": ["apps/*"]
}
```

Running `bun install` at the root installs all workspace dependencies.

---

## Current State (Scaffold)

```
redandan/
  apps/
    web/
      app/
        favicon.ico
        globals.css
        layout.tsx
        page.tsx
      components/
        ui/               ← shadcn/ui components
        theme-provider.tsx
      hooks/
      lib/
        utils.ts          ← cn() utility
      public/
      components.json     ← shadcn configuration
      eslint.config.mjs
      next.config.ts
      next-env.d.ts       ← auto-generated by Next.js (gitignored)
      package.json        ← @redandan/web
      postcss.config.mjs
      tsconfig.json
    extension/
      README.md           ← Phase 6 placeholder
  docs/
  package.json            ← workspace root
  bun.lock
```

---

## Target State (Full Build)

```
redandan/
  ├── apps/
  │   ├── web/                              ← @redandan/web
  │   │   ├── app/
  │   │   │   ├── api/                      ← Standalone REST API
  │   │   │   │   ├── auth/
  │   │   │   │   │   ├── login/route.ts
  │   │   │   │   │   ├── logout/route.ts
  │   │   │   │   │   ├── backup-code/route.ts
  │   │   │   │   │   ├── totp/
  │   │   │   │   │   │   ├── validate/route.ts
  │   │   │   │   │   │   ├── setup/route.ts
  │   │   │   │   │   │   └── confirm/route.ts
  │   │   │   │   │   └── [...nextauth]/route.ts
  │   │   │   │   ├── tasks/
  │   │   │   │   │   ├── route.ts
  │   │   │   │   │   └── [id]/route.ts
  │   │   │   │   ├── contexts/
  │   │   │   │   │   ├── route.ts
  │   │   │   │   │   └── [id]/route.ts
  │   │   │   │   ├── events/
  │   │   │   │   │   ├── route.ts
  │   │   │   │   │   └── [id]/route.ts
  │   │   │   │   ├── credentials/
  │   │   │   │   │   ├── route.ts
  │   │   │   │   │   └── [id]/route.ts
  │   │   │   │   ├── notifications/
  │   │   │   │   │   ├── route.ts
  │   │   │   │   │   └── [id]/route.ts
  │   │   │   │   ├── push/
  │   │   │   │   │   ├── subscribe/route.ts
  │   │   │   │   │   └── send/route.ts
  │   │   │   │   └── cron/
  │   │   │   │       └── notify/route.ts
  │   │   │   │
  │   │   │   ├── (auth)/                   ← Public pages (login)
  │   │   │   │   └── login/page.tsx
  │   │   │   │
  │   │   │   ├── (app)/                    ← Protected pages
  │   │   │   │   ├── layout.tsx
  │   │   │   │   ├── page.tsx              ← Dashboard
  │   │   │   │   ├── tasks/
  │   │   │   │   │   ├── page.tsx
  │   │   │   │   │   ├── new/page.tsx
  │   │   │   │   │   └── [id]/page.tsx
  │   │   │   │   ├── calendar/page.tsx
  │   │   │   │   ├── vault/
  │   │   │   │   │   ├── page.tsx
  │   │   │   │   │   ├── new/page.tsx
  │   │   │   │   │   └── [id]/page.tsx
  │   │   │   │   └── settings/page.tsx
  │   │   │   │
  │   │   │   ├── favicon.ico
  │   │   │   ├── globals.css
  │   │   │   └── layout.tsx
  │   │   │
  │   │   ├── components/
  │   │   │   ├── ui/                       ← shadcn/ui primitives
  │   │   │   ├── shared/                   ← Nav, sidebar, bottom-nav
  │   │   │   ├── tasks/
  │   │   │   ├── calendar/
  │   │   │   ├── vault/
  │   │   │   └── dashboard/
  │   │   │
  │   │   ├── lib/
  │   │   │   ├── db.ts                     ← MongoDB singleton
  │   │   │   ├── auth.ts                   ← Auth.js + validateCaller()
  │   │   │   ├── totp.ts                   ← otplib helpers
  │   │   │   ├── crypto.ts                 ← Web Crypto (client-side only)
  │   │   │   ├── push.ts                   ← Web Push (VAPID)
  │   │   │   ├── types.ts                  ← MongoDB document interfaces
  │   │   │   ├── validations.ts            ← Zod schemas
  │   │   │   └── utils.ts                  ← cn() and misc utilities
  │   │   │
  │   │   ├── hooks/
  │   │   │   ├── use-tasks.ts
  │   │   │   ├── use-vault.ts
  │   │   │   ├── use-master-key.ts
  │   │   │   └── use-notifications.ts
  │   │   │
  │   │   ├── public/
  │   │   │   └── icons/                    ← PWA icons
  │   │   │
  │   │   ├── components.json               ← shadcn config
  │   │   ├── eslint.config.mjs
  │   │   ├── middleware.ts                 ← Auth guard (edge)
  │   │   ├── next.config.ts
  │   │   ├── package.json                  ← @redandan/web
  │   │   ├── postcss.config.mjs
  │   │   ├── tsconfig.json
  │   │   └── vercel.json                   ← Cron job config
  │   │
  │   └── extension/                        ← @redandan/extension (Phase 6)
  │       ├── manifest.json                 ← Manifest V3
  │       ├── popup/
  │       │   ├── index.html
  │       │   └── popup.ts
  │       ├── background/
  │       │   └── service-worker.ts
  │       ├── content/
  │       │   └── autofill.ts
  │       ├── icons/
  │       │   ├── icon-16.png
  │       │   ├── icon-48.png
  │       │   └── icon-128.png
  │       └── package.json                  ← @redandan/extension
  │
  ├── docs/
  │   ├── README.md
  │   ├── overview.md
  │   ├── architecture.md
  │   ├── tech-stack.md
  │   ├── technical-decisions.md
  │   ├── project-structure.md
  │   ├── database.md
  │   ├── api.md
  │   ├── security.md
  │   ├── platforms.md
  │   ├── roadmap.md
  │   ├── environment.md
  │   └── agents.md
  │
  ├── .env.example
  ├── .gitignore
  ├── .prettierrc
  ├── .prettierignore
  ├── AGENTS.md
  ├── PLANNING.md
  ├── bun.lock
  └── package.json                          ← Workspace root
```

---

## Adding a New App (e.g. Mobile — Phase 7)

When Capacitor is added in Phase 7:
```
apps/
  web/
  extension/
  mobile/               ← new: @redandan/mobile
    android/
    ios/
    capacitor.config.ts
    package.json
```

Run `bun install` at the root to pick up the new workspace.

---

## Vercel Deployment

Vercel needs to know where the Next.js app lives. In the **Vercel project settings**:
- Set **Root Directory** to `apps/web`

Vercel will then:
1. Run `bun install` at the monorepo root (installs all workspace deps)
2. Run `bun run build` inside `apps/web`
3. Deploy the `apps/web/.next` output

The `vercel.json` for cron jobs lives inside `apps/web/`.

---

## Key File Responsibilities

| File | Responsibility |
|---|---|
| `apps/web/lib/db.ts` | MongoDB connection singleton. Always call `getDb()`, never instantiate MongoClient directly. |
| `apps/web/lib/auth.ts` | Auth.js config + `validateCaller(request)` — accepts both session cookies and bearer tokens. |
| `apps/web/lib/totp.ts` | otplib wrappers: generate secret, generate QR URI, verify code. |
| `apps/web/lib/crypto.ts` | Web Crypto API: PBKDF2 key derivation, AES-GCM encrypt/decrypt. Client-side only. |
| `apps/web/lib/types.ts` | TypeScript interfaces for all MongoDB document shapes. |
| `apps/web/lib/validations.ts` | Zod schemas shared between API route handlers and client forms. |
| `apps/web/middleware.ts` | Edge middleware — redirects unauthenticated requests away from `(app)` routes. |
| `apps/web/vercel.json` | Vercel Cron Job: calls `/api/cron/notify` daily at 8am. |
| `package.json` (root) | Workspace root. Scripts delegate to `apps/web`. Prettier lives here. |

---

## Naming Conventions

| Item | Convention | Example |
|---|---|---|
| Files | kebab-case | `task-card.tsx` |
| React components | PascalCase | `TaskCard` |
| Functions / variables | camelCase | `getTaskById` |
| Package names | `@redandan/<name>` | `@redandan/web` |
| API routes | plural noun | `/api/tasks`, `/api/contexts` |
| MongoDB collections | plural noun | `tasks`, `contexts` |
| Environment variables | SCREAMING_SNAKE_CASE | `MONGODB_URI` |

---

## Running the Project

```bash
# Install all workspace dependencies (run from monorepo root)
bun install

# Start the web app in development
bun dev                          # delegates to apps/web via root scripts
# or directly:
bun run --cwd apps/web dev

# Build
bun build

# Typecheck
bun typecheck
```
